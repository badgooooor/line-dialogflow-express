# line-node-dialogflow
Building some template for building chatbot with LINE and also available to use dialogflow agent to respond text messages.

## Installation and deployment
1. Edit environment variables as in .env.example and change filename to .env
2. "npm install" (or install with yarn)
